---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { siteInfo } from '../config/siteInfo';
---
<Layout title="Contact – Gehringer Conseil" description="Contactez Philippe Gehringer, manager de transition certifié Bilan Carbone, pour vos projets de transformation et de décarbonation.">
  <Header slot="header" />
   <section class="max-w-4xl mx-auto px-6 py-16 text-gray-700">
    <!-- Titre principal -->
    <h1 class="font-logo text-5xl text-center text-gray-900 mb-12">
      Me Contacter
    </h1>

    <!-- Introduction -->
    <p class="text-center text-gray-600 mb-12 max-w-2xl mx-auto leading-relaxed">
      Vous souhaitez échanger sur une mission de <strong>management de transition</strong>, un projet de <strong>développement durable</strong> ou une démarche de <strong>décarbonation</strong> ?
      <br />N’hésitez pas à me contacter directement ou via le formulaire ci-dessous.
    </p>

    <!-- Coordonnées directes -->
    <div class="text-center mb-12 space-y-2">
      <p>
        📧 <a href={`mailto:${siteInfo.email}`} class="underline hover:text-slate-900 transition">{siteInfo.email}</a>
      </p>
      <p>
        🔗 <a href={siteInfo.linkedin} target="_blank" class="underline hover:text-slate-900 transition">LinkedIn</a>
      </p>
      <p>📍 Basé à Suresnes – Île-de-France</p>
    </div>

    <!-- Formulaire de contact -->
    <form
      name="contact"
      method="POST"
      action="/merci"
      data-netlify="true"
      class="bg-gray-50 rounded-2xl shadow-md p-8 space-y-6"
    >
      <!-- Obligatoire pour Netlify -->
      <input type="hidden" name="form-name" value="contact" />

      <div>
        <label for="name" class="block text-sm font-semibold text-gray-700 mb-1">Nom *</label>
        <input
          type="text"
          id="name"
          name="name"
          required
          class="w-full border border-gray-300 rounded-lg p-3 focus:outline-none focus:ring-2 focus:ring-gray-700"
        />
      </div>

      <div>
        <label for="email" class="block text-sm font-semibold text-gray-700 mb-1">Email *</label>
        <input
          type="email"
          id="email"
          name="email"
          required
          class="w-full border border-gray-300 rounded-lg p-3 focus:outline-none focus:ring-2 focus:ring-gray-700"
        />
      </div>

      <div>
        <label for="message" class="block text-sm font-semibold text-gray-700 mb-1">Message *</label>
        <textarea
          id="message"
          name="message"
          rows="5"
          required
          class="w-full border border-gray-300 rounded-lg p-3 focus:outline-none focus:ring-2 focus:ring-gray-700"
        ></textarea>
      </div>

      <div class="text-center">
        <button
          type="submit"
          class="bg-gray-900 text-white px-8 py-3 rounded-xl hover:bg-gray-700 transition"
        >
          Envoyer le message
        </button>
      </div>
    </form>

    <!-- Mention complémentaire -->
    <p class="text-xs text-center text-gray-500 mt-8">
      Ce formulaire est traité via Netlify Forms. Aucune donnée n’est transmise à des tiers.
    </p>

    <!-- Bouton retour -->
    <div class="text-center mt-16">
      <a
        href="/"
        class="inline-block bg-gray-900 text-white px-8 py-3 rounded-xl hover:bg-gray-700 transition"
      >
        Retour à l’accueil
      </a>
    </div>
  </section>
  <Footer slot="footer" />
</Layout>