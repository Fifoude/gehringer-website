---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ServicesContent from '../content/services.md';
---


<Layout title="Tableau de Bord Solaire - APsystems">
    <style>
        .solar-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .card {
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }
        
        h1 {
            color: #667eea;
            margin-bottom: 20px;
            text-align: center;
        }
        
        h2 {
            color: #333;
            margin-bottom: 20px;
            font-size: 24px;
        }
        
        .config-section {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
        }
        
        .input-group {
            margin-bottom: 15px;
        }
        
        label {
            display: block;
            margin-bottom: 5px;
            color: #333;
            font-weight: 600;
        }
        
        input {
            width: 100%;
            padding: 12px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 14px;
            transition: border-color 0.3s;
        }
        
        input:focus {
            outline: none;
            border-color: #667eea;
        }
        
        button {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 12px 30px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
            width: 100%;
        }
        
        button:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }
        
        button:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }
        
        .tabs {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }
        
        .tab {
            background: #f8f9fa;
            padding: 10px 20px;
            border-radius: 8px;
            cursor: pointer;
            border: 2px solid transparent;
            transition: all 0.3s;
        }
        
        .tab.active {
            background: #667eea;
            color: white;
        }
        
        .tab-content {
            display: none;
        }
        
        .tab-content.active {
            display: block;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }
        
        .stat-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 20px;
            border-radius: 10px;
            color: white;
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            min-height: 120px;
        }
        
        .stat-label {
            font-size: 14px;
            opacity: 0.9;
            margin-bottom: 10px;
            white-space: nowrap;
        }
        
        .stat-value {
            font-size: 32px;
            font-weight: bold;
            white-space: nowrap;
        }
        
        .stat-unit {
            font-size: 16px;
            opacity: 0.8;
            margin-top: 5px;
            white-space: nowrap;
        }
        
        .energy-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px;
            background: #f8f9fa;
            border-radius: 8px;
            margin-bottom: 10px;
        }
        
        .energy-item-label {
            font-weight: 600;
            color: #333;
        }
        
        .energy-item-value {
            font-size: 20px;
            font-weight: bold;
            color: #667eea;
        }
        
        .energy-section {
            margin-bottom: 30px;
        }
        
        .energy-section h3 {
            color: #667eea;
            margin-bottom: 15px;
            font-size: 18px;
        }
        
        .info-card {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 10px;
            border-left: 4px solid #667eea;
        }
        
        .info-label {
            font-weight: 600;
            color: #667eea;
            margin-bottom: 5px;
        }
        
        .inverters-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }
        
        .inverter-card {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            border: 2px solid #667eea;
        }
        
        .inverter-id {
            font-weight: bold;
            color: #667eea;
            margin-bottom: 5px;
        }
        
        .error {
            background: #ff4444;
            color: white;
            padding: 15px;
            border-radius: 8px;
            margin-top: 15px;
            display: none;
        }
        
        .error.visible {
            display: block;
        }
        
        .data-section {
            display: none;
        }
        
        .data-section.visible {
            display: block;
        }

        .alert {
            background: #d1ecf1;
            border: 2px solid #0c5460;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 20px;
            color: #0c5460;
        }

        .alert-title {
            font-weight: bold;
            margin-bottom: 5px;
        }
    </style>

    <div class="solar-container">
        <div class="card">
            <h1>☀️ Tableau de Bord Solaire APsystems</h1>
            
            <div class="alert">
                <div class="alert-title">ℹ️ Information</div>
                <p>Cette page vous permet de consulter les données de votre installation solaire APsystems en temps réel.</p>
            </div>
            
            <div class="config-section">
                <div class="input-group">
                    <label for="appId">APP ID:</label>
                    <input type="text" id="appId" placeholder="Entrez votre APP ID">
                </div>
                
                <div class="input-group">
                    <label for="appSecret">APP Secret:</label>
                    <input type="password" id="appSecret" placeholder="Entrez votre APP Secret">
                </div>
                
                <div class="input-group">
                    <label for="systemId">System ID (SID):</label>
                    <input type="text" id="systemId" placeholder="Ex: AZ12649A3DFF">
                </div>
                
                <button id="loadBtn">📊 Charger les données</button>
                <div id="error" class="error"></div>
            </div>
        </div>
        
        <div id="dataSection" class="data-section card">
            <div class="tabs">
                <div class="tab active" data-tab="system">Système</div>
                <div class="tab" data-tab="energy">Énergie</div>
                <div class="tab" data-tab="balance">Bilan</div>
                <div class="tab" data-tab="inverters">Onduleurs</div>
            </div>
            
            <div id="systemTab" class="tab-content active">
                <h2>Informations du Système</h2>
                <div id="systemInfo"></div>
            </div>
            
            <div id="energyTab" class="tab-content">
                <h2>Production d'Énergie</h2>
                <div id="energyStats"></div>
            </div>

            <div id="balanceTab" class="tab-content">
                <h2>Bilan Énergétique du Jour</h2>
                <div id="balanceStats"></div>
            </div>
            
            <div id="invertersTab" class="tab-content">
                <h2>Onduleurs</h2>
                <div id="invertersList"></div>
            </div>
        </div>
    </div>

    <script>
        let config = {
            appId: '',
            appSecret: '',
            systemId: ''
        };

        // Gestion des onglets
        document.querySelectorAll('.tab').forEach(tab => {
            tab.addEventListener('click', () => {
                const tabName = tab.dataset.tab;
                document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
                document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
                tab.classList.add('active');
                document.getElementById(tabName + 'Tab').classList.add('active');
            });
        });

        function showError(message) {
            const errorEl = document.getElementById('error');
            errorEl.textContent = message;
            errorEl.classList.add('visible');
            setTimeout(() => errorEl.classList.remove('visible'), 5000);
        }

        async function makeRequest(endpoint, params = {}) {
            const queryString = new URLSearchParams({
                appId: config.appId,
                appSecret: config.appSecret,
                ...params
            }).toString();

            const response = await fetch(`/.netlify/functions/apsystems?endpoint=${encodeURIComponent(endpoint)}&${queryString}`);
            
            if (!response.ok) {
                const error = await response.json();
                throw new Error(error.error || 'Erreur de requête');
            }
            
            return await response.json();
        }

        async function loadSystemData() {
            config.appId = document.getElementById('appId').value.trim();
            config.appSecret = document.getElementById('appSecret').value.trim();
            config.systemId = document.getElementById('systemId').value.trim();
            
            if (!config.appId || !config.appSecret || !config.systemId) {
                showError('Veuillez remplir tous les champs');
                return;
            }
            
            const btn = document.getElementById('loadBtn');
            btn.disabled = true;
            btn.textContent = '⏳ Chargement...';
            
            try {
                // Charger les détails du système
                const systemDetails = await makeRequest(`/user/api/v2/systems/details/${config.systemId}`);
                if (systemDetails.code !== 0) {
                    throw new Error(`Erreur API (${systemDetails.code})`);
                }
                displaySystemInfo(systemDetails.data);
                
                // Charger les données d'énergie
                const energySummary = await makeRequest(`/user/api/v2/systems/summary/${config.systemId}`);
                if (energySummary.code !== 0) {
                    throw new Error(`Erreur API (${energySummary.code})`);
                }
                displayEnergySummary(energySummary.data);
                
                // Charger les onduleurs
                const inverters = await makeRequest(`/user/api/v2/systems/inverters/${config.systemId}`);
                if (inverters.code !== 0) {
                    throw new Error(`Erreur API (${inverters.code})`);
                }
                displayInverters(inverters.data);
                
                // Charger les compteurs (meters)
                const meters = await makeRequest(`/user/api/v2/systems/meters/${config.systemId}`);
                if (meters.code === 0 && meters.data && meters.data.length > 0) {
                    // Charger les données du premier compteur
                    const meterId = meters.data[0];
                    const meterSummary = await makeRequest(`/user/api/v2/systems/${config.systemId}/devices/meter/summary/${meterId}`);
                    if (meterSummary.code === 0) {
                        displayEnergyBalance(meterSummary.data);
                    }
                } else {
                    // Si pas de compteur, afficher un message
                    document.getElementById('balanceStats').innerHTML = `
                        <div class="alert" style="background: #fff3cd; border-color: #ffc107; color: #856404;">
                            <div class="alert-title">ℹ️ Aucun compteur détecté</div>
                            <p>Votre système ne semble pas avoir de compteur activé. Le bilan énergétique n'est pas disponible.</p>
                        </div>
                    `;
                }
                
                document.getElementById('dataSection').classList.add('visible');
                
            } catch (error) {
                showError(error.message);
                console.error(error);
            } finally {
                btn.disabled = false;
                btn.textContent = '📊 Charger les données';
            }
        }

        function displaySystemInfo(data) {
            const types = {
                1: 'Système PV',
                2: 'Système de stockage',
                3: 'Système PV & Stockage'
            };
            
            const html = `
                <div class="info-card">
                    <div class="info-label">System ID</div>
                    <div>${data.sid}</div>
                </div>
                <div class="info-card">
                    <div class="info-label">Date de création</div>
                    <div>${data.create_date}</div>
                </div>
                <div class="info-card">
                    <div class="info-label">Capacité</div>
                    <div>${data.capacity} kW</div>
                </div>
                <div class="info-card">
                    <div class="info-label">Type</div>
                    <div>${types[data.type] || 'Inconnu'}</div>
                </div>
                <div class="info-card">
                    <div class="info-label">Fuseau horaire</div>
                    <div>${data.timezone}</div>
                </div>
                <div class="info-card">
                    <div class="info-label">ECU(s)</div>
                    <div>${data.ecu.join(', ')}</div>
                </div>
            `;
            document.getElementById('systemInfo').innerHTML = html;
        }

        function displayEnergySummary(data) {
            const html = `
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-label">Aujourd'hui</div>
                        <div class="stat-value">${parseFloat(data.today).toFixed(2)}</div>
                        <div class="stat-unit">kWh</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-label">Ce mois</div>
                        <div class="stat-value">${parseFloat(data.month).toFixed(2)}</div>
                        <div class="stat-unit">kWh</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-label">Cette année</div>
                        <div class="stat-value">${parseFloat(data.year).toFixed(2)}</div>
                        <div class="stat-unit">kWh</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-label">Total</div>
                        <div class="stat-value">${parseFloat(data.lifetime).toFixed(2)}</div>
                        <div class="stat-unit">kWh</div>
                    </div>
                </div>
            `;
            document.getElementById('energyStats').innerHTML = html;
        }

        function displayEnergyBalance(data) {
            const today = data.today || {};
            const produced = parseFloat(today.produced || 0);
            const consumed = parseFloat(today.consumed || 0);
            const imported = parseFloat(today.imported || 0);
            const exported = parseFloat(today.exported || 0);
            const selfConsumed = produced - exported;
            const selfConsumptionRate = produced > 0 ? (selfConsumed / produced * 100) : 0;
            const autonomyRate = consumed > 0 ? (selfConsumed / consumed * 100) : 0;

            const html = `
                <div class="energy-section">
                    <h3>📊 Flux énergétiques</h3>
                    <div class="energy-item">
                        <span class="energy-item-label">☀️ Énergie produite</span>
                        <span class="energy-item-value">${produced.toFixed(2)} kWh</span>
                    </div>
                    <div class="energy-item">
                        <span class="energy-item-label">🏠 Énergie consommée</span>
                        <span class="energy-item-value">${consumed.toFixed(2)} kWh</span>
                    </div>
                    <div class="energy-item">
                        <span class="energy-item-label">⬆️ Énergie injectée (réseau)</span>
                        <span class="energy-item-value">${exported.toFixed(2)} kWh</span>
                    </div>
                    <div class="energy-item">
                        <span class="energy-item-label">⬇️ Énergie importée (réseau)</span>
                        <span class="energy-item-value">${imported.toFixed(2)} kWh</span>
                    </div>
                </div>

                <div class="energy-section">
                    <h3>📈 Indicateurs</h3>
                    <div class="stats-grid" style="grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));">
                        <div class="stat-card" style="background: linear-gradient(135deg, #28a745 0%, #20c997 100%);">
                            <div class="stat-label">Autoconsommation</div>
                            <div class="stat-value">${selfConsumed.toFixed(2)}</div>
                            <div class="stat-unit">kWh</div>
                        </div>
                        <div class="stat-card" style="background: linear-gradient(135deg, #ffc107 0%, #ff9800 100%);">
                            <div class="stat-label">Taux d'autoconso.</div>
                            <div class="stat-value">${selfConsumptionRate.toFixed(0)}</div>
                            <div class="stat-unit">%</div>
                        </div>
                        <div class="stat-card" style="background: linear-gradient(135deg, #17a2b8 0%, #138496 100%);">
                            <div class="stat-label">Taux d'autonomie</div>
                            <div class="stat-value">${autonomyRate.toFixed(0)}</div>
                            <div class="stat-unit">%</div>
                        </div>
                    </div>
                </div>

                <div style="background: #e7f3ff; padding: 15px; border-radius: 8px; margin-top: 20px;">
                    <p style="margin: 0; color: #004085; font-size: 14px;">
                        <strong>💡 Conseil :</strong> Un taux d'autoconsommation élevé indique que vous utilisez directement l'énergie que vous produisez. 
                        Un taux d'autonomie élevé indique que vous dépendez peu du réseau électrique.
                    </p>
                </div>
            `;
            document.getElementById('balanceStats').innerHTML = html;
        }

        function displayInverters(data) {
            let html = '';
            data.forEach(ecu => {
                html += `<h3 style="color: #667eea; margin-top: 20px;">ECU: ${ecu.eid}</h3>`;
                html += `<div class="inverters-grid">`;
                ecu.inverter.forEach(inv => {
                    html += `
                        <div class="inverter-card">
                            <div class="inverter-id">${inv.uid}</div>
                            <div>Type: ${inv.type}</div>
                        </div>
                    `;
                });
                html += `</div>`;
            });
            document.getElementById('invertersList').innerHTML = html;
        }

        document.getElementById('loadBtn').addEventListener('click', loadSystemData);
    </script>
</Layout>